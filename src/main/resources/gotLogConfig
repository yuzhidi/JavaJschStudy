username=
password=123
host=10.10.10.75
targetFilePath=/tmp/logFile
logDir=
command="cd $logDir; for i in `ls -t roc.log*`;do echo \$i;grep -q $keyWord \$i; if [[ \$? == 0 ]]; then echo found:\$i;break; fi;done"
